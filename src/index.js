/**
 * @file BdScroll插件主文件
 * @author simmons8616(simmons0616@gmail.com)
 */

export default class BdScroll {
    static Version = '1.0.0';

    constructor(options = {}) {
        console.log(options);
    }
}
